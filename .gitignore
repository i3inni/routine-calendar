# ── 비밀 값 (절대 커밋 금지) ──
Secrets.xcconfig
# APNs 인증 키
*.p8
.env
server/.env

# ── iOS / Xcode (사용자별 상태) ──
xcuserdata/
*.xcuserstate
DerivedData/
.DS_Store

# ── Spring Boot / Gradle ──
server/build/
server/.gradle/
