# ── 비밀 값 (절대 커밋 금지) ──
Secrets.xcconfig
*.p8                 # APNs 인증 키
.env
server/.env

# ── iOS / Xcode ──
RoutineCalendar.xcodeproj   # xcodegen이 project.yml로 생성
xcuserdata/
*.xcuserstate
DerivedData/
.DS_Store

# ── Spring Boot / Gradle ──
server/build/
server/.gradle/
